# ═══════════════════════════════════════════════════════════════
#  ZeroBot v2 — Requirements (NSE India)
#  Python 3.10+ | pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ── Web Framework & Dashboard ──────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
websockets==13.1

# ── PostgreSQL (PgAdmin4 compatible) ──────────────────────────
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# ── Data & Finance ─────────────────────────────────────────────
yfinance>=0.2.50
pandas>=2.2.3
numpy>=1.26.0
pandas-ta==0.4.71b0

# ── Market Calendar (NSE Holidays) ─────────────────────────────
pytz==2024.2
holidays==0.59

# ── Machine Learning ───────────────────────────────────────────
scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.5.0
joblib==1.4.2
scipy>=1.11.0                  # Black-Scholes option pricing (norm.cdf)

# ── Statistical Arbitrage ──────────────────────────────────────
statsmodels>=0.14.0            # Engle-Granger cointegration

# ── HTTP & Async ───────────────────────────────────────────────
aiohttp==3.11.10
requests==2.32.3
httpx==0.28.0

# ── Telegram Alerts ────────────────────────────────────────────
python-telegram-bot==22.6

# ── Config & Env ───────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
pyyaml==6.0.2

# ── Shoonya / S-Mode (required for S-Mode WebSocket ticks) ──────
NorenRestApiPy                 # Shoonya (Finvasia) WebSocket API — S-Mode tick feed
pyotp==2.9.0                   # TOTP 2FA for Shoonya + Angel One login

# ── Angel One SmartAPI (uncomment when going live) ─────────────
# ⚠️  IMPORTANT: Use 'smartapi-python' NOT 'SmartApi'
#     WRONG:   pip install SmartApi        ← breaks with PyCrypto error
#     CORRECT: pip install smartapi-python ← works on Windows no compiler
smartapi-python==1.3.4
pycryptodome                 # Replaces PyCrypto — works on Windows without C++

# ── Utilities ──────────────────────────────────────────────────
loguru==0.7.3
rich==13.9.4
colorama==0.4.6

# ── Scheduling ─────────────────────────────────────────────────
apscheduler==3.10.4

# ── Testing ────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0

# ── FinBERT (OPTIONAL — run install_finbert.bat to install) ─────────────────
# Off by default. Enables AI-grade financial news sentiment scoring.
# ~500MB download (one-time). ~2GB RAM needed while running.
# After installing: set ZEROBOT_USE_FINBERT=1 in config/.env
#
# To install manually:
#   pip install "transformers>=4.35.0"
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# transformers>=4.35.0
# torch>=2.0.0     # CPU-only: --index-url https://download.pytorch.org/whl/cpu

# Angel One SmartAPI is now always installed (required for hybrid/live mode)
groq>=0.28.0  # Groq LLM API (optional — enhances Gates 6 & 11)

# ── Timezone data (Linux cloud servers) ─────────────────────────────────
tzdata>=2024.1  # zoneinfo timezone database for Linux
